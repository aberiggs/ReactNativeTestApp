import {StyleSheet} from 'react-native';

const textStyle = StyleSheet.create({
    big: {
        color: '#ffffff',
        fontSize: 40,
        textAlign: 'center'
    },
    med: {
        color: '#ffffff',
        fontSize: 30,
        textAlign: 'center',
        padding: 10
    },
    smallMed: {
        color: '#ffffff',
        fontSize: 20,
        textAlign: 'center',
        padding: 10
    },
    small: {
        color: '#ffffff',
        fontSize: 15,
        textAlign: 'center',
        padding: 10
    }

})

export {textStyle}